{
  "name": "lobstermusic-club",
  "version": "1.0.0",
  "description": "🦞 The Official OpenClaw Theme Song Contest",
  "scripts": {
    "dev": "wrangler pages dev ./public --d1 DB",
    "deploy": "wrangler pages deploy ./public",
    "db:create": "wrangler d1 create lobstermusic",
    "db:init": "wrangler d1 execute lobstermusic --file=./schema.sql",
    "db:init:local": "wrangler d1 execute lobstermusic --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
